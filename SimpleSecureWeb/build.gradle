plugins {
    id 'java'
    id 'war'
}

dependencies {
    compileOnly("javax.servlet:javax.servlet-api:4.0.1")
}

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}

war {
    webAppDirectory = file('WebContent')
}